<Window x:Class="PRN212_PROJECT.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PRN212_PROJECT"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Login" Height="450" Width="800">
    <Grid Background="WhiteSmoke" Margin="20">
        <Border Background="White" 
    CornerRadius="8" 
    Padding="30"
    Width="393"
    VerticalAlignment="Center"
    HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Welcome Back" 
           FontSize="24" 
           FontWeight="Bold" 
           Margin="0,0,0,20"/>
                <TextBlock Text="Your Phone Number" Margin="0,0,0,5"/>
                <TextBox x:Name="txtPhoneNumber" Height="30"/>
                <TextBlock Text="Password" Margin="0,15,0,5"/>
                <PasswordBox x:Name="pwdBox" Height="30"/>
                <Button Content="Login" 
        Height="35" 
        Margin="0,20,0,0"
        Command="{Binding LoginCommand}" Click="Button_Click"/>
                <TextBlock HorizontalAlignment="Center" Margin="0,15,0,0">
  <Run Text="Don't have an account? "/>
  <Hyperlink Click="HyperlinkRegister_Click">
    Register(This use only for customer)
  </Hyperlink>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</Window>
